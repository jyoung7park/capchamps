_schema-version: 3.3.0
ID: productshop
description: A simple CAP project.
version: 1.0.0
modules:
- name: productshop-srv
  type: nodejs
  path: gen/srv
  requires:
  - name: productshop-auth
  - name: productshop-db
  provides:
  - name: srv-api
    properties:
      srv-url: ${default-url}
  parameters:
    buildpack: nodejs_buildpack
    instances: 1
  build-parameters:
    builder: npm-ci
- name: productshop-db-deployer
  type: hdb
  path: gen/db
  requires:
  - name: productshop-db
  parameters:
    buildpack: nodejs_buildpack
- name: productshop-destination-content
  type: com.sap.application.content
  requires:
  - name: productshop-destination-service
    parameters:
      content-target: true
  - name: productshop_html_repo_host
    parameters:
      service-key:
        name: productshop_html_repo_host-key
  - name: productshop-auth
    parameters:
      service-key:
        name: productshop-auth-key
  parameters:
    content:
      instance:
        destinations:
        - Name: productshop_productshop_html_repo_host
          ServiceInstanceName: productshop-html5-app-host-service
          ServiceKeyName: productshop_html_repo_host-key
          sap.cloud.service: productshop
        - Authentication: OAuth2UserTokenExchange
          Name: productshop_productshop_auth
          ServiceInstanceName: productshop-auth
          ServiceKeyName: productshop-auth-key
          sap.cloud.service: productshop
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: productshop-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: productshop_html_repo_host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - nsproducts.zip
      name: nsproducts
      target-path: resources/
- name: nsproducts
  type: html5
  path: app/products
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: productshop-auth
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      oauth2-configuration:
        credential-types:
        - binding-secret
        - x509
      tenant-mode: dedicated
      xsappname: productshop-${org}-${space}
    path: ./xs-security.json
    service: xsuaa
    service-plan: application
- name: productshop-db
  type: com.sap.xs.hdi-container
  parameters:
    service: hana
    service-plan: hdi-shared
- name: productshop-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          - Authentication: NoAuthentication
            HTML5.DynamicDestination: true
            HTML5.ForwardAuthToken: true
            Name: srv-api
            ProxyType: Internet
            Type: HTTP
            URL: ~{srv-api/srv-url}
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: productshop-destination-service
    service-plan: lite
  requires:
  - name: srv-api
- name: productshop_html_repo_host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: productshop-html5-app-host-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
  - builder: custom
    commands:
    - npm ci
    - npx cds build --production
